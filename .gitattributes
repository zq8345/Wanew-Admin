# ── 行尾：工作树字节 == 仓库字节 ──────────────────────────────────────────────
# 起因（2026-07-28）：core.autocrlf=true 让「我写出来的文件」是 LF、「git 检出的文件」是 CRLF，
# 同一棵工作树里两种混着。后果不是显示问题，是**闸的判定反转**：
# token-lint 剥行尾注释的正则在 CRLF 下整条匹配失败（点号不匹配 CR，无 m 的行尾锚只认字符串真结尾），
# 于是那句写着"字节数，不是 content.length"的**注释本身**被当成了违规 —— 我这棵树绿、部署树红，
# **同一个 commit 两套字节**，而这道闸从没在一次全新检出上跑过。
#
# ⇒ 钉死行尾，让「闸在谁的树上跑」这个变量消失，而不是每道闸各自记得归一（第三条规则必然忘）。
# ⚠️ 仓库里本来就全是 LF（`git ls-files --eol` 35 个 i/lf、0 个 CRLF），所以这是一次**声明**，
#    不是一次内容改动：`git add --renormalize .` 的 diff 为空。
* text=auto eol=lf

# ── 逐字节镜像：不参与任何行尾转换 ────────────────────────────────────────────
# 这两条**必须放在上面那行之后**（后写的规则优先）。它们是官网真源的逐字节镜像，
# 守卫用 Buffer 比较，任何转换都会造成假漂移。⚠️ 也正因为它们钉死成 LF，
# 读 vendor/github.js 的那两道闸（成本模型、上线就绪）里的 `\n}` 才没在部署树上炸 ——
# **那不是正则写对了，是这一行保护的。删它会让两道闸同时静默失效。**
vendor/** -text
# 官网令牌层切片镜像：同 vendor/** 钉死字节，防 autocrlf 造假漂移
public/w3-tokens.css -text
